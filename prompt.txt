The current code use segmentation model and get the area of floor. Given this, I would like to add arrows to the image like the example image. Where the arrows starts from a point below the middle bottom of the image, and points to the navigatable points on the boundries of the segmentation mpas. The arrows should not be too close to each other. And the final output image should not have the segmentation map, but only keep the arrows, with each of them numbered from 1, 2, 3, ... in circles. There should be an circled 0 besides these arrows, indicating turning over. The idea is that we can let a vlm to choose from 0,1,2,3 for a robot navigation task, so that the vlm's job is easier. You can take reference from implementation of @src . Modify @add_action_proposal_projection_fixed.py  please.

